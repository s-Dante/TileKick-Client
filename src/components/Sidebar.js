import {navigate} from '@/core';

export function Sidebar () {
    const sidebar = document.createElement('nav');
    const title = sidebar.appendChild('span');

    title.textContent = 'TILEKICK';

    const links = sidebar.appendChild('ul');

}