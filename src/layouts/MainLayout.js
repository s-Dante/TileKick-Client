import { navigate } from '@/core';

import { Sidebar } from '@/components';

export function MainLayout(contentHTML, title) {
    const mainLayout = document.createElement('nav');
    mainLayout.className('sidebar');
    mainLayout.appendChild();
}